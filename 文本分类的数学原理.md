# 文本分类的数学原理

## TF - IDF

TF - IDF 通过给每个词定义权重实现文本到特征向量的转变

### 原理

给定文档集合 $D =\{d_1,...,d_m\}$ 其中 $d_i$ 在这个项目里表示一句文本

词频 $tf(t,d) = \frac{词 t 在文档 d 中出现的次数}{文档 d 的总词数}$

逆文档频率 $idf(t,D) = log\frac{1+m}{1+df(t)} + 1$ 其中 $df(t)$ 表示词 $t$ 出现的文本数（此例中指句数），$m$ 为文档数。

TF - IDF 值：$w_{t,d} = tf(t,d) * idf(t,D)$

> 为什么TF - IDF值能精准地表示每个词的权重呢？两者之间有何关联？

TF 衡量的是某个词在当前文档中出现的频率。相当于 TF 越大，这个词出现的就越多。但是如果仅仅以 TF 为参考，那类似“的”“了”的词会拿到极高的权重，所以需要 IDF 来弱化这种普遍性。IDF 相当于评价一个词在整个文档集中的稀有程度。类似“的”“了”在文档集中的IDF会很低。

### 代码

```python
#TF - IDF获取词频数据

vectorizer = TfidfVectorizer(max_features = 5000, ngram_range = (1,2))
X_train = vectorizer.fit_transform(df_train['token'])
X_test = vectorizer.transform(df_test['token'])
```



## 逻辑回归

### 原理

在常规的线性回归的基础上，逻辑回归通过取 $\sigma(z)$ 将线性输出映射在 $[0,1]$ ，用于二分化后的回归拟合
$$
ln(\frac{p}{1-p}) = w^Tx + b
$$
$p = P(y = 1 | x)$ 表示元素属于二分化后的其中一类的概率，则 $\frac{p}{1-p}$ 表示几率
$$
\frac{p}{1-p} = e^{w^Tx + b} \\
令 t = e^{w^Tx+b} 则，\\
p = t - tp\\
p = \frac{t}{1+t}\\
p = \sigma(w^Tx + b) = \frac{1}{1+e^{-(w^Tx+b)}}
$$
预测时，可以通过 $p$ 值得到该元素输入哪类（比如 $>0.5$ 设为一类，$<0.5$ 设为二类）

## 损失函数与优化

### 原理

我们从两个角度分别看待这个优化问题

#### 最大似然估计

MLE 的基本思想是找到一组 $w$ 使这组参数对 $\hat{y}$ 的估计尽可能准确

单个样本的似然：
$$
P(y|x;w) = \hat{y}^y(1-\hat{y})^{1-y}
$$
其中 $P(y|x;w)$ 表示在使用参数 $w$ 时，遇到样本 $x$ 时估计到正确 $\hat{y}$ 的概率

比如 $y = 1$ 若 $\hat{y}= 1$ 则$P = 1$ 若 $\hat{y} = 0$ 则 $P = 0$

$y=0$ 亦然

各样本独立，整个数据集的似然函数是：
$$
L(w) = \Pi^n_{i = 1}\hat{y}_i^{y_i}(1-\hat{y}_i)^{1-y_i} 
$$
  连乘不好优化，取对数得到对数似然（单调变换，极值点不变）：
$$
l(w) = logL(w) = \Sigma^n_{i = 1}[y_ilog\hat{y}_i+(1-y_i)log(1-\hat{y_i})]
$$
最大似然估计就是让 $l(w)$ 最大

#### 交叉熵损失最小

真实分布：$[y,1-y]$

预测分布：$[\hat{y},1-\hat{y}]$

使用这两个概率分布建立交叉熵：
$$
H(p,q) = -\Sigma_kp(x)ln(q) = -log\hat{y} - (1-y)log(1-\hat{y})
$$
所有样本的平均损失就是：
$$
J(w) = -\frac{1}{n}\Sigma_{i = 1}^n[y_ilog\hat{y_i} + (1-y_i)log(1-\hat{y_i})]
$$
其实 $-l(w) = nJ(w)$

两者是等价的

#### 梯度下降法

虽然梯度下降本质贪心，但 $J(w)$ 是个光滑的凸函数，所以梯度下降是正确的

- 计算梯度 $\nabla{J}$ 即最陡上升方向
- 往反方向（负梯度方向）走一步
- 不断重复

用数学语言就是
$$
w: =w - \alpha\nabla{J(w)}
$$
对于：
$$
J(w) = -\Sigma_{i = 1}^n[y_ilog\hat{y_i} + (1-y_i)log(1-\hat{y_i})]
$$
其中 $\hat{y_i} = \sigma(w^Tx_i) = \frac{1}{1+e^{-w^Tx_i}}$
这里将 $\frac{1}{n}$ 进行忽略处理，因为对梯度的大小没有影响

需要计算 $\nabla{J}$ 即 $\frac{\partial{J}}{\partial{w}}$

单个样本求偏导：
$$
\frac{\partial}{\partial{w}}[-(ylog\hat{y} + (1-y)log(1-\hat{y}))] = (\hat{y} - y)x
$$
对所有样本求和，得：
$$
\nabla{J(w)} = \Sigma_{i = 1} ^{n}(\hat{y_i}-y_i)x_i
$$
其中 $(\hat{y} - y)$ 是预测误差

直观地可以看出：

若误差 > 0，即 $\hat{y}- y > 0$ 此时参数会沿负梯度方向减小，即让 $\hat{y}$ 变小

误差 < 0 同理

附上求偏导过程
$$
\sigma'(x) = (1-\sigma(x))\sigma(x)
$$

$$
令z=w^Tx\\
\frac{\part{L}}{\part{z}}=\frac{\partial}{\partial{z}}-[ylog\sigma{(z)} + (1-y)log(1-\sigma{(z)})] = -(\frac{y}{\sigma{(z)}} - \frac{1-y}{1-\sigma(z)})\sigma{(z)}(1-\sigma{(z)}) = -y(1-\sigma{(z)}) +(1-y)\sigma(z) = \sigma{(z)} - y
$$

$$
因为 \frac{\part{z}}{\part{w}} = x \\
所以 \frac{\part{L}}{\part{w}} = (\sigma(z) - y)x = (\hat{y} - y) x
$$

这里需要解释一下为什么将 $z$ 定义为 $w^Tx$ 而不是 $w^Tx + b$

可以在特征向量 $x$ 的末尾加一个1，此时
$$
\tilde{x} = \begin{bmatrix} x  \\ 1 \end{bmatrix},\tilde{w} = \begin{bmatrix} w  \\ b \end{bmatrix},z = \tilde{w}^T\tilde{x}
$$
关于增广特则的合法性及惩罚，就不在这里写了

###  代码

```python
#逻辑回归进行模型训练
clf = linear_model.LogisticRegression(max_iter = 100,random_state = 42)
clf.fit(X_train,df_train['label'])
```

## 混淆矩阵与指标

### 原理

创建的混淆矩阵有四个不同的象限：

- True Negative 真负（左上角象限）
- False Positive 假正（右上角象限）
- False Negative 假负（左下角象限）
- True Positive 真正（右下角象限）

True 意味着值被准确地预测了，False 意味着存在错误或错误的预测

#### 准确性

准确性衡量模型正确的频率

(True Positive + True Negative) / Total Predictions

#### 精确度

在预测为正的结果中，真正为正的比例

True Positive / (True Positive + False Positive)

#### 敏感性

在所有正例中，预测为正的比例

True Positive / (True Positive + False Negative)

#### 特异性

模型在预测反例方面的表现

True Negative / (True Negative + False Positive)

#### F-分数

F 分数是精确度和敏感性的调和平均数

2 * ((Precision * Sensitivity) / (Precision + Sensitivity))

### 代码

```python
#混淆矩阵评估
y_pred = clf.predict(X_test)
print("\nThe report:\n")
print(metrics.classification_report(df_test['label'], y_pred))
print("\n The confusion matrix:\n")
print(metrics.confusion_matrix(df_test['label'], y_pred))
```

## 网格搜索

### 原理

没什么好说的，尝试不同的值，使模型最精准

### 代码

```python
param_grid = {
    'tfidf__max_features':[3000, 5000],
    'tfidf__ngram_range' : [(1,1), (1,2)],
    'tfidf__min_df' : [1,2],
    'clf__C' : [0.1, 1, 10],
    'clf__solver' : ['lbfgs']
}

#网格搜索 + 5折交叉认证
grid_search = GridSearchCV(pipeline, param_grid, cv = 5, scoring = 'f1_macro', n_jobs = -1, verbose = 1)
grid_search.fit(df_train['token'],y_train)

print("The best varieties", grid_search.best_params_)
print("The best exchange examine F1(macro) : {:.4f}".format(grid_search.best_score_))


best_model = grid_search.best_estimator_
y_pred = best_model.predict(df_test['token'])
```

